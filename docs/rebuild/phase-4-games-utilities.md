# BILM Full Rebuild — Phase 4 (Games + Utility)

## Implemented
- Rebuilt Games domain index into modular foundation-wired assets (`games/index.html`, `games/style.css`, `games/script.js`).
- Added grouped catalog rendering, inline search/filtering, and cleaner game launch card flow.
- Rebuilt utility pages under `random/`:
  - `rng.html` (random generator utility)
  - `reset.html` (local storage reset utility)

## Notes
- Existing `games/play.html` route remains compatible and is still used for launching playable content.
